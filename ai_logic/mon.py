from __future__ import annotations
from ai_logic.common import c_yellow, tag

def handle_mon(argv: list[str], cfg: dict) -> int:
    print(f"{tag('MON', c_yellow())} belum diisi (stub).")
    return 0
